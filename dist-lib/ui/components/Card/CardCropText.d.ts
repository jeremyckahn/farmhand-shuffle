import { ICrop, IPlayedCrop } from '../../../game/types';
interface CropCardTextProps {
    crop: ICrop;
    playedCrop?: IPlayedCrop;
}
export declare const CardCropText: ({ crop, playedCrop }: CropCardTextProps) => import("react/jsx-runtime").JSX.Element;
export {};
